# Mortgage Calculator Core

Shared TypeScript source for mortgage calculations, formatting helpers, and translations used by both app surfaces:

- `apps/web` — Vite + React web UI deployed to GitHub Pages
- `apps/mobile` — Expo + React Native mobile UI shell

The source is imported directly by the app packages today so the existing root web build and GitHub Pages deployment can remain simple.
